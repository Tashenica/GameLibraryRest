# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: gamelibrary-api
metadata:
  template: gamelibrary-api@0.0.1-beta

services:
  api:
    project: ./GameLibraryApi
    host: appservice
    language: dotnet
